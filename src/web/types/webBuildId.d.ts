/**
 * Injetado via `define` em vite.web.config.ts — hash curto do commit (ou
 * timestamp, se não houver git) do build web que está rodando. Ver
 * src/web/updaterWeb.ts.
 */
declare const __WEB_BUILD_ID__: string
